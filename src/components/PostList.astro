---
import TagList from './TagList.astro';
import { formatPostDate, getPostHref, getPostTags, type BlogPost } from '../lib/posts';

interface Props {
  posts: readonly BlogPost[];
}

const { posts } = Astro.props;
---

{
  posts.length === 0 ? (
    <p>No notes yet.</p>
  ) : (
    <ul class="post-list">
      {posts.map((post) => (
        <li>
          <time datetime={post.data.pubDate.toISOString()}>{formatPostDate(post)}</time>{' '}
          <a href={getPostHref(post)}>{post.data.title}</a>
          <TagList tags={getPostTags(post)} label={`Tags for ${post.data.title}`} />
        </li>
      ))}
    </ul>
  )
}
